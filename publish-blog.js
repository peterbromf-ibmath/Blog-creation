#!/usr/bin/env node 'use strict'; const admin = require('firebase-admin'); const TOPIC_ROTATION = [ { angle: 'path-to-7-ladder', course: 'hlaa', label: 'HL AA — the Path-to-7 mastery ladder explained (with example)', cta: 'Try Path to 7 →', url: '/path-to-7.html', image: '/blog/images/hlaa-formulas-to-memorise.jpg' }, { angle: 'ai-marking-p2', course: 'slaa', label: 'SL AA Paper 2 — how AI marking spots the method marks students miss', cta: 'Try AI marking →', url: '/slaa.html', image: '/blog/images/how-ai-grading-improves-marks.jpg' }, { angle: 'ia-feedback', course: 'hlai', label: 'AI IA feedback — grade your Internal Assessment in 60 seconds', cta: 'Grade my IA →', url: '/ia-feedback.html', image: '/blog/images/ia-topic-ideas-that-examiners-love.jpg' }, { angle: 'hardest-questions', course: 'slai', label: 'SL AI — the 5 hardest questions in Unit 3 (Financial Maths) and how to nail them', cta: 'Practise Unit 3 →', url: '/slai.html', image: '/blog/images/cg50-statistics-slai-paper2.jpg' }, { angle: 'preib-diagnostic', course: 'preib', label: 'PreIB Prep — the 30-minute diagnostic that predicts your DP1 grade', cta: 'Try the diagnostic →', url: '/preib.html', image: '/blog/images/igcse-0580-to-ib-maths.jpg' }, { angle: 'sat-vs-ib-transferable', course: 'sat', label: "SAT Digital Math vs IB Maths — what transfers, what doesn't", cta: 'See SAT Prep →', url: '/sat.html', image: '/blog/images/sat-math-for-ib-students.jpg' }, { angle: 'predicted-papers', course: 'hlai', label: 'How the Predicted Papers builder tailors a mock from your weakest topics', cta: 'Try Predicted Papers →', url: '/predicted-papers.html', image: '/blog/images/predicted-paper-may-2027-slaa.jpg' }, { angle: 'live-game-classroom', course: 'any', label: 'Turning revision into a classroom game — the Live Game + Jeopardy setup', cta: 'Play Live Game →', url: '/game.html', image: '/blog/images/common-misconceptions-september.jpg' }, { angle: 'flashcards-spaced', course: 'any', label: 'Spaced-repetition flashcards for IB Maths — the 6-day rhythm that works', cta: 'Open flashcards →', url: '/flashcards.html', image: '/blog/images/memorise-trig-identities.jpg' }, { angle: 'checklist-coverage', course: 'hlaa', label: 'The syllabus checklist trick — spot the topics you thought you\'d covered', cta: 'Open checklist →', url: '/checklist.html', image: '/blog/images/daily-study-routine-ib-maths.jpg' }, { angle: 'school-licences', course: 'school', label: "Whole-department IB Maths licences — what's included and what's not", cta: 'See school pricing →', url: '/schools.html', image: '/blog/images/why-i-teach-both-aa-and-ai.jpg' }, { angle: 'referral-invite', course: 'any', label: 'Invite a classmate, both get €10 off — how the referral works', cta: 'Get your link →', url: '/referrals.html', image: '/blog/images/parents-guide-supporting-ib-maths.jpg' } ]; const BASE_URL = 'https://ibmathrevision.com'; const argv = process.argv.slice(2); const args = { dryRun: false, topicIdx: null }; for (const a of argv) { if (a === '--dry-run') args.dryRun = true; else if (a.startsWith('--topic=')) args.topicIdx = parseInt(a.slice(8), 10); } function initFirebase() { if (admin.apps.length) return admin.app(); const serviceAccount = JSON.parse(process.env.FIREBASE_SERVICE_ACCOUNT); return admin.initializeApp({ credential: admin.credential.cert(serviceAccount), }); } function weekIndex(date = new Date()) { const start = Date.UTC(2026, 0, 5); return Math.max(0, Math.floor((date.getTime() - start) / (7 * 24 * 3600 * 1000))); } function pickTopic() { if (args.topicIdx !== null && !Number.isNaN(args.topicIdx)) { return TOPIC_ROTATION[((args.topicIdx % TOPIC_ROTATION.length) + TOPIC_ROTATION.length) % TOPIC_ROTATION.length]; } const now = new Date(); const w = weekIndex(now); const isThursday = now.getUTCDay() === 4; const idx = ((w * 2) + (isThursday ? 1 : 0)) % TOPIC_ROTATION.length; return TOPIC_ROTATION[idx]; } function slugify(s) { return String(s).toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-+|-+$/g, '').slice(0, 80); } async function generateArticle(apiKey, topic) { const url = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:generateContent?key=${encodeURIComponent(apiKey)}`; const prompt = `You are Pete Bromfield, an IB Maths teacher. Write a punchy blog post (600 words) on: ${topic.label}. Return valid JSON: { "title": "...", "subtitle": "...", "html": "...", "socialCaption": "...", "callToAction": "${topic.cta}" }`; const body = { contents: [{ role: 'user', parts: [{ text: prompt }] }] }; const maxAttempts = 4; for (let attempt = 1; attempt <= maxAttempts; attempt++) { try { const res = await fetch(url, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(body) }); const json = await res.json(); if (!res.ok) { const isRetryable = res.status === 503 || res.status === 429; console.warn(`[publish-blog] Gemini attempt ${attempt} failed: HTTP ${res.status} — ${JSON.stringify(json).slice(0, 200)}`); if (isRetryable && attempt < maxAttempts) { const waitMs = attempt * 15000; console.log(`[publish-blog] Retrying in ${waitMs / 1000}s...`); await new Promise(r => setTimeout(r, waitMs)); continue; } throw new Error(`Gemini API error ${res.status}: ${JSON.stringify(json)}`); } const text = json?.candidates?.[0]?.content?.parts?.[0]?.text || ''; if (!text) throw new Error('Gemini returned an empty response.'); const s = text.indexOf('{'); const e = text.lastIndexOf('}'); return JSON.parse(text.slice(s, e + 1)); } catch (err) { if (attempt === maxAttempts) throw err; console.warn(`[publish-blog] Gemini attempt ${attempt} failed: ${err.message}`); await new Promise(r => setTimeout(r, attempt * 15000)); } } throw new Error('Gemini generation failed after all retries.'); } async function bufferGraphQL({ token, query }) { const res = await fetch('https://api.buffer.com', { method: 'POST', headers: { 'Authorization': `Bearer ${token}`, 'Content-Type': 'application/json' }, body: JSON.stringify({ query }) }); const json = await res.json(); if (json.errors) throw new Error(json.errors[0].message); return json.data; } function gqlString(s) { return String(s || '').replace(/\\/g, '\\\\').replace(/"/g, '\\"').replace(/\n/g, '\\n'); } async function createDraft({ token, profileId, text, photoUrl }) { const mediaFragment = photoUrl ? `, media: { photo: "${gqlString(photoUrl)}" }` : ''; const query = `mutation { createDraft(input: { profileId: "${gqlString(profileId)}", content: { text: "${gqlString(text)}"${mediaFragment} } }) { draft { id } } }`; const data = await bufferGraphQL({ token, query }); return { id: data.createDraft.draft.id }; } async function publishToFirestore({ documentId, title, subtitle, html, callToAction, callToActionUrl }) { initFirebase(); const db = admin.firestore(); await db.collection('blog_articles').doc(documentId).set({ title, subtitle, html, publishedAt: admin.firestore.FieldValue.serverTimestamp(), author: 'Pete Bromfield', callToAction: callToAction || 'Read more →', callToActionUrl: callToActionUrl || '/', }); } async function main() { const { BUFFER_ACCESS_TOKEN, BUFFER_PROFILE_ID, GEMINI_API_KEY } = process.env; if (!GEMINI_API_KEY || !BUFFER_ACCESS_TOKEN || !BUFFER_PROFILE_ID) { console.error('Missing env vars.'); process.exit(1); } const topic = pickTopic(); const article = await generateArticle(GEMINI_API_KEY, topic); const documentId = `blog-${weekIndex()}-${slugify(article.title)}`; const articleUrl = `${BASE_URL}/blog.html?id=${documentId}`; const draftText = `${article.socialCaption}\n\nRead more: ${articleUrl}`; const photoUrl = topic.image ? `${BASE_URL}${topic.image}` : null; try { await publishToFirestore({ documentId, title: article.title, subtitle: article.subtitle, html: article.html, callToAction: article.callToAction, callToActionUrl: topic.url, }); console.log(`[publish-blog] Published to Firestore: ${documentId}`); } catch (e) { console.error('Firestore publish failed:', e.message); process.exit(4); } try { const draft = await createDraft({ token: BUFFER_ACCESS_TOKEN, profileId: BUFFER_PROFILE_ID, text: draftText, photoUrl }); console.log(`[publish-blog] Done. Draft created: ${draft.id}${photoUrl ? ' (with image: ' + photoUrl + ')' : ''}`); } catch (e) { console.error('Buffer draft failed:', e.message); process.exit(3); } } main().catch(e => { console.error(e); process.exit(1); }); 
